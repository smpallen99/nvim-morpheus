return {
  component = require "morpheus.utils.status.component",
  condition = require "morpheus.utils.status.condition",
  env = require "morpheus.utils.status.env",
  heirline = require "morpheus.utils.status.heirline",
  hl = require "morpheus.utils.status.hl",
  init = require "morpheus.utils.status.init",
  provider = require "morpheus.utils.status.provider",
  utils = require "morpheus.utils.status.utils",
}
